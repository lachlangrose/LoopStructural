[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "loop-visualization"
description = "Visualization utilities for LoopStructural"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["numpy", "pyvista"]